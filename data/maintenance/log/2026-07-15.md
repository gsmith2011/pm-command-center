# Maintenance review — 2026-07-15 (`/review` full sweep)

First full maintenance sweep. Covers the ~3-week activation investigation (2026-06-24 → 2026-07-14) that ran from symptom (flat 58%) to a decided direction (sync-health detection v1).

## 1. Direct edits applied
**None required.** INDEXes, `Last touched` fields, and cross-links were maintained inline during each ingest, so there was no drift to fix. Verified this sweep:
- All relative `.md` links in content files resolve (the only unresolved paths are fictional examples inside `hypotheses/_SCHEMA.md` / `decisions/_SCHEMA.md` DO/DON'T blocks — expected, not rot).
- `hypotheses/INDEX.md` matches file statuses (field-mapping `promoted`; onboarding + setup `active`).
- `decisions/INDEX.md` lists the 2026-07-14 decision under Recently decided.
- Stakeholder INDEX last-touched dates match the files.

## 2. Requires PM judgment (surfaced, NOT auto-applied)

### T1 — now concrete, time-boxed, and decision-linked
`strategy.md § Tensions § T1` still reads "open — raise with Marcus/Jordan before next planning cycle." It is now sharper than that: **decision [2026-07-14-sync-health-detection-v1](../../decisions/2026-07-14-sync-health-detection-v1.md) depends on a leadership deprioritization (drop SFDC-depth OR Enterprise scoping) that has NOT happened**, with a self-supersede date of **2026-07-28**. Marcus is actively pushing the opposite (Enterprise). **Recommend** updating `strategy.md § T1` to reference the decision, the forcing date, and the two candidate victims — *not* resolving it. This is on the "Ask the PM before" list (strategy edits / tension resolution), so it's proposed, not done.

### Relationship debt — Jordan Reyes (high influence, never touched)
[jordan-reyes.md](../../stakeholders/jordan-reyes.md) is still a TODO stub, last-touched `—`, and is now **on the critical path**: co-owner (with Marcus) of the deprioritization sign-off the decision needs by 2026-07-28. **Recommend** a touchpoint before then. Secondary: [renee-park.md](../../stakeholders/renee-park.md) (last touched 2026-06-24, ~3 wks) is the owner of the funnel-segmentation the decision commissions — worth re-engaging.

### The escalation is the #1 open action
The decision is `decided` (direction) but its execution is blocked on an un-started escalation to Marcus/Jordan. Because it's not `status: pending`, it won't trip the decision-debt auto-flag — so noting it here explicitly: **if the PM doesn't secure sign-off by 2026-07-28, the decision self-supersedes.** Owner: PM.

### Onboarding — confirm deferred, not rejected
[onboarding-ux-friction.md](../../hypotheses/onboarding-ux-friction.md) is correctly `active` + co-equal validated + deferred. The commissioned **Renee funnel-stage pull** (never-started vs. worked-then-broke) has no owner-date yet — recommend booking it so onboarding gets sized for its own decision rather than quietly lapsing.

## 3. Knowledge gap suggestions
- **No `knowledge/product/features/` files exist.** Hypotheses reference `salesforce-sync.md` / `new-account-onboarding.md` (backticked, not links). Now that there's a decision, a `features/salesforce-sync.md` (or `sync-health.md`) canonical feature file is a natural home for v1 scope + status.
- **Metrics still TODO:** activation measurement-window (Renee) and CRM sync success rate are unfilled — the detection instrumentation this decision builds is what will finally populate them.

## 4. Hygiene note (transparency)
During `/hypothesize` I wrote `maintenance/log/2026-07-14.md`, overwriting a pre-existing **untracked** file of the same name that existed at session start. It was never committed, so nothing in git history was lost — but if intended content was in that placeholder, it's unrecoverable. Flagging for PM confirmation.

## 5. No new strategy tension
The Marcus Enterprise-vs-activation pressure is a facet of T1, not a separate tension. Nothing else meets the recurring + high-confidence + decision-relevant threshold.
